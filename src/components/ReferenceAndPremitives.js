// Reference is when we assign a value to a variable or object then its called reference

const numbers = 1;

// premitives are when we copy the same value of a variable its actualy point to exact value of object or variable in memory

const premetivenumber = numbers;

console.log(premetivenumber);


// for object

const perosn = {

  name : 'asif'
};

const secondPerson = perosn;

console.log(secondPerson)